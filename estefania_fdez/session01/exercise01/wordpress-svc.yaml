apiVersion: v1
kind: Service
metadata:
  name: wordpress-svc
  namespace: exercise-01
  labels:
    app: wordpress
  annotations:
      nginx.ingress.kubernetes.io/affinity: cookie
spec:
  ports:
    - port: 80
  selector:
      app: wordpress
