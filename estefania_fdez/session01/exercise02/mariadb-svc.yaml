apiVersion: v1
kind: Service
metadata:
  name: mariadb-svc
  namespace: exercise-02
  labels:
    app: mariadb
spec:
  type: ClusterIP
  ports:
    - port: 3306
  targetPort: 3306
  selector:
    app: mariadb
