apiVersion: v1
kind: Service
metadata:
  name: wordpress-svc
  namespace: exercise-02
  labels:
    app: wordpress
  annotations:
      nginx.ingress.kubernetes.io/affinity: cookie
spec:
  ports:
    - port: 80
  targetPort: 80
  selector:
      app: wordpress
