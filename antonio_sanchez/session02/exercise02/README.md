# Session02 - Exercise02

The objetive is add capabilities to the basic chart in order to convert **db** *Deployment* type to *StatefulSet* type and use if you like of the Autoscaling feature.

## Tips

* **--set hpa.enable=false** to disable Autoscaling
* Change **db-deployment.yaml** to **db-statefulset.yaml**
* Fully tested
